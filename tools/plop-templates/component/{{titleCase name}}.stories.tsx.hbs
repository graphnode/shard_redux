import { Story, Meta } from '@storybook/react';

import {{titleCase name}}, { {{titleCase name}}Props } from './{{titleCase name}}';

export default {
  title: '{{titleCase name}}',
  component: {{titleCase name}},
} as Meta;

const Template: Story<{{titleCase name}}Props> = (args) => (
  <{{titleCase name}} {...args} />
);

export const Default = Template.bind({});
Default.args = {

};
