import React, { useEffect, useRef, useState } from 'react';

import * as S from './{{titleCase name}}.styles';

export interface {{titleCase name}}Props {

}

const {{titleCase name}} : React.FC<{{titleCase name}}Props> = (props) => {
  return (
    <S.Container>
      Hello {{titleCase name}}!
    </S.Container>
  );
};

{{titleCase name}}.defaultProps = {

};

export default {{titleCase name}};
